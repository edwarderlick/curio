# OS
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.swp

# Safety net — app/ and server/ have their own .gitignore for node_modules,
# dist, and .env within each package; this catches anything created at the
# repo root by mistake.
node_modules/
dist/
.env
.env.*.local
*.log
